import { Typography } from '@material-ui/core';
import React from 'react';

const PageTitle = ({ text }) => <Typography variant="h4" style={{ color: '#F2184F', fontWeight: 700, marginBottom: 30 }}>{text}</Typography>

export default PageTitle;